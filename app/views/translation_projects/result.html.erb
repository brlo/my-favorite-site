<section id="page-content" class="looks-like-page text-page" itemscope="" itemtype="http://schema.org/Article">

<div id="chapter-header" data-controller="menu-filter">
  <h2 class="parent-title">
    <%= link_to 'Работа над переводом', translation_project_path(@project, part: @part, lang_to: @lang_to) %>
  </h2>
  <div id="h1-title">
    <h1
      class="title"
      id="current-address"
      data-book-short-name="<%= request.path %>"
      data-lang-ui="<%= I18n.locale %>"
      data-lang-content="<%= @lang_to %>"
      data-book-code=""
      data-page-path=""
      data-disable-chapters="<%= @is_disable_chapters ? 1 : 0 %>"
      data-chapter="<%= @part %>"
      itemprop="headline"
    >
      <%= @translated_title.presence || @project.title %>
    </h1>
  </div>

  <div id="title-sub">
    <strong><%= translation_lang_by_code(@project.source_langs.first) %> → <%= translation_lang_by_code(@lang_to) %></strong><br>
    Перевод подготавливается пользователями сайта bibleox.com.<br>
    <% if !logged_in? %> Вы можете помочь улучшить этот текст! <%= link_to 'Зарегистрируйтесь', signup_path %> и присоединяйтесь к работе над <%= link_to 'переводом', translation_project_path(@project, part: @part, lang_to: @lang_to) %>. <br><% end %>
    Как и материалы Wikipedia, результат распространяется свободно под лицензией CC BY-SA.
  </div>

  <div class='small hint' style='text-align: center'>Часть</div>
  <nav id='menu-chapters'>
    <% prev_chapter = @part - 1 %>
    <% now_chapter = @part %>
    <% next_chapter = @part + 1 %>
    <% @all_parts.each do |i| %>
      <% classes = "chapter-num" %>
      <% i == now_chapter ? classes = ' selected' : nil %>
      <a id="ch-<%= i %>" data-chapter="<%= i %>" class="<%= classes %>" href="<%= result_translation_project_path(@project, part: i, lang_to: @lang_to) %>"><%= i %></a>
    <% end %>
  </nav>

  <div id="text-bar-wrapper">
    <div class='bar-description'>Перевод на язык:</div>
    <div id='text-bar'>
      <div class='bar-el'>

        <div data-controller="bbx-select"
            data-bbx-select-action-value="navigate"
            data-bbx-select-navigate-path-value="<%= "/#{I18n.locale}/translate/#{@project.id}/result?part=#{@part}&lang_to=" %>">
          <select data-bbx-select-target="select" name="locale" id="page-lang-select" data-page='true' class='search-select'>
            <% langs_for_translates(is_with_encient: false).sort_by { |lang, _| [-@progress_for_all_langs.fetch(lang, 0), lang] }.each do |lang, name| %>
              <% next if @all_langs.exclude?(lang) %>
              <% next if @project.source_langs.to_a.include?(lang) %>
              <option value='<%= lang %>' <%= 'selected' if @lang_to == lang %> data-progress="<%= @progress_for_all_langs[lang] %>"><%= name %></option>
            <% end %>
          </select>
          <div data-bbx-select-target="custom"></div>
        </div>

      </div>

      <div class='right-group'>
        <div class='bar-el btn' data-controller='share-btn' data-action='click->share-btn#share'>
          <a class='share-btn'>
            <svg style="width: 15px;" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
              <path stroke-linecap="round" stroke-linejoin="round" d="M7.217 10.907a2.25 2.25 0 1 0 0 2.186m0-2.186c.18.324.283.696.283 1.093s-.103.77-.283 1.093m0-2.186 9.566-5.314m-9.566 7.5 9.566 5.314m0 0a2.25 2.25 0 1 0 3.935 2.186 2.25 2.25 0 0 0-3.935-2.186Zm0-12.814a2.25 2.25 0 1 0 3.933-2.185 2.25 2.25 0 0 0-3.933 2.185Z" />
            </svg>
          </a>
        </div>
      </div>
    </div>
  </div>

</div>

  <article
    id='page-body'
    data-controller='share-links'
    itemprop="articleBody"
    class="verses <%= text_cont_classes %> <%= text_size_class %>">

    <%= @result_html.html_safe %>
    <hr>
    <i>Работу над переводом можно продолжить в <%= link_to 'специальном разделе', translation_project_path(@project, part: @part, lang_to: @lang_to) %>.</i>
  </article>

  <% if @references_html.length > 10 %>
    <section id='page-references' class="looks-like-page <%= text_cont_classes %>">
      <h2 id="page-references-title"><%= ::I18n.t('page.references') %></h2>
      <div id="page-references-text">
        <%= @references_html.html_safe %>
      </div>
    </section>
  <% end %>
</section>

<%= render(partial: 'shared/arrow_to_top') %>
